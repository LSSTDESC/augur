version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  conda:
    file: environment.yml
  jobs:
    post_install:
      - pip install -U sphinx numpydoc sphinx-rtd-theme
      - pip install -e .

sphinx:
  configuration: conf.py
